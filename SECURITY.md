# セキュリティ

このアプリが何から守ろうとしていて、何は守れないのかをまとめます。

---
<br/>
<br/>

## 守ろうとしているもの
| 想定する相手 | 対策 |
|---|---|
| 公開URLを知った第三者 | アプリは設定を持たない状態では初期設定画面しか出さない。文書の取得には利用者のトークンが要る |
| APKを入手した第三者 | APKにも設定は入っていない。起動しても初期設定画面が出るだけ |
| 端末を短時間触る人 | 最後の認証から120時間でロックがかかる。手動での即時ロックもできる |
| 文書に紛れ込んだスクリプトや外部リソース | 追加時にワークフローが弾き、表示時にもサニタイズ・CSP・sandboxの3層で無効化する |
| トークンが漏れた場合の影響 | 対象リポジトリ1つの読み取りだけに限定している。書き込みも他リポジトリへのアクセスもできない |
| ロック画面に出る通知からの情報漏れ | 通知に文書名・ファイル名・リポジトリ名を含めない |

---
<br/>
<br/>

## 守れないもの
次は対策の範囲外です。仕様上の残余リスクとして受け入れています。

- root化・脱獄した端末
- OSの管理者権限を持つ相手、フォレンジックによる取得
- 悪意あるブラウザ拡張
- GitHubアカウントそのものの乗っ取り
- 利用者がトークンやパスワードを他人へ渡した場合
- 120時間の有効期間中に、端末のロックも解除された状態で操作された場合
- 画面の撮影、肩越しの覗き見

---
<br/>
<br/>

## 秘密情報の置き場所
| 値 | 置き場所 | 公開されるか |
|---|---|---|
| Fine-grained personal access token | 端末のIndexedDB（AES-GCMで暗号化） | されない |
| アプリ専用パスワード | どこにも保存しない | されない |
| master key | パスワードと端末鍵の二重で包んで保存 | されない |
| Web Push の秘密鍵 | 文書リポジトリのActions Secret | されない |
| Android署名鍵 | このリポジトリのActions Secret | されない |
| Web Push の公開鍵 | `public/runtime-config.json` | 公開（秘密ではない） |
| 署名証明書のフィンガープリント | `assetlinks.json` | 公開（仕様上必要） |

CIの `npm run verify:secrets` が、追跡対象ファイルとビルド成果物に対して、トークンらしき文字列、秘密鍵のヘッダー、キーストア、`.env` の実ファイル、そして文書リポジトリ名が含まれていないかを毎回検査します。1件でも見つかればビルドを失敗させます。

---
<br/>
<br/>

## 暗号の設計
| 項目 | 値 | 理由 |
|---|---|---|
| master key | 256bit AES-GCM | 設定と既読情報の暗号化に使う |
| 鍵導出 | PBKDF2-HMAC-SHA-256、600,000回 | オフラインでの総当たりを現実的でなくする |
| salt | 16バイトの暗号学的乱数 | 事前計算表を無効にする |
| IV | 暗号化のたびに96bit乱数 | 同じ平文でも暗号文が変わる |
| 追加認証データ | `schemaVersion\|recordKey\|algorithmVersion` | あるレコードの暗号文を別のレコードへ差し替える改ざんを検出する |
| 端末鍵 | 取り出し不可（`extractable: false`） | IndexedDBを覗いても鍵の値を読み出せない |

パスワードを変更しても master key は作り直しません。新しいパスワード由来の鍵で包み直すだけなので、保存済みのデータを平文へ展開する必要がありません。

---
<br/>
<br/>

## 文書表示の隔離
文書HTMLは次の3層を通してから表示します。1層が破られても、残りが効きます。

### 1層目：サニタイズ
`script` `iframe` `object` `embed` `form` `base` `link` `meta` を取り除き、すべての `on*` 属性、`javascript:` 等の危険なURL、外部を指すリソース参照、CSS内の外部 `url()` と `@import` を落とします。ページ内リンク以外の `a href` も外します。

### 2層目：文書内CSP
`default-src 'none'` を基点に、`data:` と `blob:` の画像・メディア・フォント、インラインCSSだけを許可します。`connect-src 'none'` により、仮にスクリプトが動いたとしても外部へは通信できません。

### 3層目：sandbox iframe
`sandbox=""` の iframe へ `srcdoc` として渡します。`allow-scripts` も `allow-same-origin` も `allow-forms` も `allow-top-navigation` も付けません。スクリプトは実行されず、親のDOMにも触れず、フォームも送信できません。

文書の本文は表示中だけメモリに置きます。閉じたときに `srcdoc` を空にし、Cache Storage・IndexedDB・localStorage のいずれにも保存しません。

---
<br/>
<br/>

## 配信元が改ざんされた場合
公開しているPWAが書き換えられると、利用者が入力したトークンを盗まれる可能性があります。これに対しては次で備えています。

- `main` ブランチへの直接の変更を避け、CIを通す
- GitHub Pages へのデプロイはGitHub Actionsからだけ行う
- デプロイ用ワークフローの権限を必要最小限にする
- トークンを対象リポジトリ1つの読み取りだけに限定する
- 設定画面にビルド元のコミットSHAを表示し、意図しない版が配信されていないか確認できるようにする

GitHubアカウントには2要素認証またはパスキーを設定してください。この対策が最後の砦になります。

---
<br/>
<br/>

## 仕様から補正した点
実装にあたって、要件をより安全側へ寄せた箇所です。

### 相対URLのリンクも無効化する
要件は「非fragmentの `a[href]`」を無効化するよう定めています。相対URLも外部取得の起点になり得るため、`#` で始まるもの以外はすべて無効化しています。

### 相対パスの `@import` も禁止する
要件は外部の `@import` を禁じています。相対パスであっても自己完結型という前提が崩れるため、`@import` は無条件に取り除いています。

### アプリシェルのCSPで `data:` を許可する
`srcdoc` の iframe は埋め込み元のCSPを継承します。アプリシェル側で `data:` を塞ぐと、文書に埋め込まれた画像やフォントが表示できなくなるため、`data:` と `blob:` を許可しています。外部への通信は許可していません。

### `frame-ancestors` は指定していない
このアプリを他サイトの iframe へ埋め込ませない指定ですが、レスポンスヘッダーでしか解釈されません。GitHub Pages ではヘッダーを設定できないため、`meta` へ書いても無視されます。効かない指定を残すと、守れているという誤解を生むうえコンソールにエラーが出るため、外しています。

埋め込まれた場合に問題になるのは、外側のページが入力を盗み見る類の攻撃です。これに対しては、そもそも外側のページを用意できるのは配信元を掌握した相手だけであり、その時点で `frame-ancestors` の有無は意味を持ちません。配信元の保護（下記）が実質的な対策になります。

---
<br/>
<br/>

## 問題を見つけた場合
このアプリは1人で使うことを前提にした個人用のものです。公開の窓口はありません。問題に気づいたら、利用を止めてトークンを失効させ、必要ならアプリをリセットしてください。
